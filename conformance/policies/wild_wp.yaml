apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: wp
  importDerivedRoles: [glob_roles]
  rules:
    - actions: ['view']
      effect: EFFECT_ALLOW
      derivedRoles: ['STARRED']
